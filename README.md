# Telegram VPN Key Bot

Telegram-бот для выдачи VPN-ключей (**VLESS + Reality**) через **3x-ui API**  
с поддержкой ролей, логированием, балансом и админ-командами.

---

## 📌 О проекте

Бот позволяет **создавать, пересоздавать, удалять и выдавать VPN-ключи** в Telegram,  
используя панель **3x-ui**.

Проект реализован как **v1 (MVP)** с упором на стабильность, аудит и расширяемость.

### Используемый стек
- Node.js
- Telegraf
- Sequelize
- Node-Cache
- 3x-ui API

---

## 📂 Структура проекта

- /logs — логирование сообщений и действий
- /uploads — сохранение медиа-файлов (по датам)
- /services — бизнес-логика (VPN, логирование, правила, доступ)
- /routes — команды и callback-логика бота
- bot.js — точка входа
- config.js — конфигурация проекта
- database.js — подключение БД и модели
- .env — переменные окружения


---

## 🧭 Routes (`/routes`)

- `index.js` — автозагрузка всех доступных путей с защитой от ошибок
- `main.js` — пользовательские команды
- `admin.js` — админ-команды
- `promo.js` — промокоды
- `payment.js` — баланс и платежи

---

## ⚙️ Режимы работы

Бот может работать в одном из двух режимов:

### PROD (по умолчанию)
- Основной рабочий режим
- Без сброса базы данных
- Стандартное логирование

### DEV
- Режим тестирования
- Сброс базы данных при каждом запуске
- Логирование всех транзакций с базой данных
- Принудительное логирование чатов

---

## 🛠 Конфигурация (`config.js`)

### `tgbot`
- `apikey` — **обязательный** Telegram Bot API Token
- `mode` — `PROD` / `DEV`
- `admin` — суперадминистратор
  - `id` - id главного пользователя 
  - `username`- username для ссылок
- `group` — ограничения по Telegram-группе
  - `id` - id группы
  - `name` - отображаемое название группы
  - `username` - username группы
  - `guard` — требовать участие в группе
- `privacy` — логирование сообщений и медиа
- `version` — версия бота
- `rules` — правила, обязательные к принятию

---

### `db`
- `host` — хост базы данных
- `name` — имя базы данных
- `user` — пользователь
- `password` — пароль
- `dialect` — (`mysql`, `mariadb`, `postgres`, `mssql`)
- `prefix` — префикс таблиц

---

### `vpn`
Каждая VPN-локация описывается отдельным объектом:

- `status` — активна ли локация
- `name` — отображаемое имя (`ru` / `en`)
- `desc` — описание
- `roleTime` — срок действия ключей по ролям (мс)
- `host`, `login`, `password`, `path`, `port` — доступ к 3x-ui
- `inbounds`
  - поддерживается **только VLESS + Reality**
  - `id`
  - `publickey`
  - `sni`
  - `sid`
  - `port`

---

## 🚀 Функционал

### 👑 Админ-команды

- /admin — админ-панель
- /userdata — информация о пользователях
- /msg — сообщение пользователю
- /set — изменить роль
- /get — данные пользователя
- /ban — блокировка
- /unban — разблокировка
- /money — управление балансом
- /roles — список ролей

### 👤 Пользовательские команды

- /start — главное меню
- /help — помощь
- /faq — информация
- /promo — активировать промокод
- /pay — пополнение баланса
- /notify — уведомления
- /status — статус VPN

---

## 🔘 Callback-события

- `home` — главное меню
- `vpn` — список локаций
- `vpn_<name>_locate` — информация о локации
- `vpn_<name>_create` — создать ключ
- `vpn_<name>_reload` — пересоздать ключ
- `rules_accept` / `rules_dont_accept`
- `close`

---

## 📥 Установка и запуск

```bash
git clone https://github.com/Py6hok/vpn-key-bot.git
cd vpn-key-bot
npm install

